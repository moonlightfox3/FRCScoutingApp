Needs
    Server - Supabase
        Add button to submit LocalStorage - form type=submit button? (also clears LocalStorage on success, tells user the result)
        Add button to directly submit match data without using LocalStorage

Wants

Luxuries

Testing
    Does device sleep if using only keyboard or controller?
        Keyboard on phone
        Controller on phone

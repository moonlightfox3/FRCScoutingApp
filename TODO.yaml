Needs
    Server - Supabase
        Client UI
            View data
            Submit data
                Submit LocalStorage (also clears LocalStorage files on success)
                Directly submit match data without using LocalStorage
            Change data
            Remove data

Wants

Luxuries

Testing
    Does device sleep if using only keyboard or controller?
        Keyboard on phone
        Controller on phone

Needs
    Server - Supabase
        Managed manually, not with the API
        Database
            Table for list of all events, including which one is the current event (also a testing event)
            Table for each event
        Add button to submit LocalStorage - form type=submit button? (also clears LocalStorage on success, tells user the result)

Wants

Luxuries

Testing
    Does device sleep if using only keyboard or controller?
        Keyboard on phone
        Controller on phone

Needs
    Server - Supabase
        Client UI - Data summary page
            View data
            Change data
            Remove data
    UI
        Dropdowns instead of radio buttons (also more approximation probably)
        Bigger buttons, text inputs, custom number inputs, dropdowns, checkboxes, text

Wants

Luxuries

Testing

unsorted
    custom url or maybe don't use github pages (github pages is blocked on school chromebooks)
